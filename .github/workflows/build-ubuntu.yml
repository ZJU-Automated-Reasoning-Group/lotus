name: Build Lotus on Linux
on:
  push:
  pull_request:
jobs:
  build-linux:
    runs-on: ${{ matrix.ubuntu-version }}
    timeout-minutes: 60
    strategy:
      matrix:
        ubuntu-version: [ubuntu-22.04, ubuntu-24.04, ubuntu-latest]
        compiler: [clang, gcc]
        build-type: [Release, Debug]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies and setup LLVM
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends \
            gcc g++ cmake libgmp-dev libboost-all-dev \
            libz3-dev python3 python3-pip llvm-14 llvm-14-dev llvm-14-tools clang-14 \
            libgtest-dev libgmock-dev ccache
          # Setup LLVM environment
          echo "LLVM_CONFIG=/usr/bin/llvm-config-14" >> $GITHUB_ENV
          
      - name: Setup compiler (clang)
        if: matrix.compiler == 'clang'
        run: |
          echo "CC=/usr/bin/clang-14" >> $GITHUB_ENV
          echo "CXX=/usr/bin/clang++-14" >> $GITHUB_ENV
          
      - name: Setup compiler (gcc)
        if: matrix.compiler == 'gcc'
        run: |
          echo "CC=/usr/bin/gcc" >> $GITHUB_ENV
          echo "CXX=/usr/bin/g++" >> $GITHUB_ENV

      - name: Cache compiler results
        uses: actions/cache@v4
        with:
          path: ~/.cache/ccache
          key: ${{ runner.os }}-${{ matrix.ubuntu-version }}-${{ matrix.compiler }}-${{ matrix.build-type }}-ccache-v1-${{ hashFiles('**/CMakeLists.txt', '**/*.cmake') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.ubuntu-version }}-${{ matrix.compiler }}-${{ matrix.build-type }}-ccache-v1-

      - name: Build and install
        run: |
          set -euo pipefail
          export CCACHE_BASEDIR="$GITHUB_WORKSPACE"
          export CCACHE_COMPILERCHECK=content
          ccache --zero-stats
          trap 'ccache --show-stats' EXIT

          debug_flags=()
          if [[ "${{ matrix.build-type }}" == "Debug" ]]; then
            # Keep assertions and unoptimized Debug behavior, but avoid duplicating
            # full debug information into every statically linked tool and test.
            debug_flags+=(
              "-DCMAKE_C_FLAGS_DEBUG=-O0 -g0"
              "-DCMAKE_CXX_FLAGS_DEBUG=-O0 -g0"
            )
          fi

          mkdir -p build && cd build
          cmake -DCMAKE_INSTALL_PREFIX="$HOME/lotus" \
                -DCMAKE_BUILD_TYPE="${{ matrix.build-type }}" \
                -DLLVM_BUILD_PATH="/usr/lib/llvm-14/lib/cmake/llvm" \
                -DLLVM_CONFIG_PATH="/usr/bin/llvm-config-14" \
                -DLLVM_DIR="/usr/lib/llvm-14/lib/cmake/llvm" \
                -DZ3_DIR="/usr" \
                -DENABLE_SEAHORN=OFF \
                -DLOTUS_BUILD_TESTS=ON \
                -DLOTUS_ENABLE_CLAM=ON \
                -DCMAKE_C_COMPILER_LAUNCHER=ccache \
                -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
                "${debug_flags[@]}" ..
          cmake --build . --parallel "$(nproc)"
          cmake --install .
      - name: Add Lotus to the path
        run: |
          echo "$HOME/lotus/bin" >> $GITHUB_PATH
      - name: Test installation
        run: |
          test -x "$HOME/lotus/bin/lotus-check"
          lotus-check --help >/dev/null
