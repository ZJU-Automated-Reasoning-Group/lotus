# SPEC CPU2006 bitcode suite

Whole-program LLVM bitcode inputs intended for explicit scalability and
performance experiments. They are not default build or unit-test inputs.

The repository does not currently contain a maintained benchmark harness or
complete provenance/license metadata for these binaries. Do not publish or
redistribute results until those details and the exact tool command are
recorded.
