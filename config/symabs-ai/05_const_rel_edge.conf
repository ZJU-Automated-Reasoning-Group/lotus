# Configuration: Constant + Relations - Edge Level
# Original: 05_const_rel_edge.py
# Combined constant propagation and relational analysis

FragmentDecomposition.Strategy = Edges
AbstractDomain = SimpleConstProp, NumRels.Unsigned
ParamStrategy = AllValuePairs(symmetric=true)

# Description:
# - Product domain: constants + unsigned relations
# - Per-edge transformers
# - More precise than constant propagation alone

