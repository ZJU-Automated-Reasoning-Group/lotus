# Configuration: Memory Region Analysis
# Original: memregion.py
# Comprehensive memory region tracking

MemoryModel.Variant = BlockModel
FragmentDecomposition.Strategy = Headers
Analyzer.WideningDelay = 20000

AbstractDomain = MemRegion, NumRels.Signed, NumRels.Zero
ParamStrategy.NumRels.Signed = NonPointerPairs(symmetric=true)
ParamStrategy.NumRels.Zero = NonPointers

# Description:
# - Block-based memory model
# - Memory region tracking with MemRegion domain
# - Signed relations for non-pointer values
# - Zero tracking for all non-pointers
# - Very high widening delay for precision
# - Header-level abstraction

