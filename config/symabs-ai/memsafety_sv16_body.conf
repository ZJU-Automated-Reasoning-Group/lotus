# Configuration: Memory Safety (SV-COMP 2016) - Body
# Original: memsafety_sv16_body.py
# Optimized for SV-COMP memory safety benchmarks

MemoryModel.Variant = BlockModel
FragmentDecomposition.Strategy = Body
Analyzer.WideningDelay = 9999

AbstractDomain = MemRegion, NumRels.Signed, NumRels.Zero
ParamStrategy.NumRels.Signed = NonPointerPairs(symmetric=true)
ParamStrategy.NumRels.Zero = NonPointers

# Description:
# - Designed for SV-COMP memory safety category
# - Block-based memory model
# - Body-level abstraction for loop handling
# - High widening delay (9999) for precision
# - Combines memory regions and numerical relations
# - Tracks zero values separately

