# Configuration: Memory Safety (SV-COMP 2016) - Header
# Original: memsafety_sv16_header.py
# Header-level abstraction for memory safety

MemoryModel.Variant = BlockModel
FragmentDecomposition.Strategy = Headers
Analyzer.WideningDelay = 9999

AbstractDomain = MemRegion, NumRels.Signed, NumRels.Zero
ParamStrategy.NumRels.Signed = NonPointerPairs(symmetric=true)
ParamStrategy.NumRels.Zero = NonPointers

# Description:
# - SV-COMP memory safety with header abstraction
# - Block-based memory model
# - Header-level abstraction at loop entry points
# - High widening delay for precision
# - Same domain as body variant but different abstraction points

